# EVVora

Electronic health record for Minnesota 245D-licensed providers (home and community-based services).

## Stack
Next.js 16 (App Router, TypeScript) · Tailwind v4 · npm. Node lives at `/opt/homebrew/bin` (not on the non-interactive PATH).

## Design tokens
Palette and type mimic notion.com (sampled 2026-09-02 from Notion's "tatami" CSS variables). All colors are defined once in `src/app/globals.css` and exposed as Tailwind classes (`bg-sidebar`, `text-muted`, `bg-accent`, `border-line`, `gray-100`…`gray-900`). Do not hardcode hex values in components.

- White page, warm-gray surfaces: `sidebar` #f9f9f8, `panel` #f6f5f4, `line` #dfdcd9. Hover/active are black at 4%/6%.
- Text is black at 0.898 opacity (`text`), 0.95 for headings (`text-strong`), 0.54 for secondary (`muted`).
- `accent` blue #0075de is the only accent: primary buttons, links, selected states. `accent-soft` #e6f3fe for secondary buttons.
- `ok` (teal) / `warn` (yellow) / `danger` (red), each with a `-soft` tint, for status.

Type: Inter (Notion ships a custom Inter build called NotionInter). Base 14px/20px, UI labels weight 500, headings weight 700 with tight tracking (h1 32/40 -0.75px, h2 22/28 -0.25px). Serif (`font-serif`, Georgia stack standing in for Lyon Text) for editorial moments only.

## UI primitives
Build every screen from `src/components/ui.tsx`; do not hand-roll cards, tables, or form grids in pages.
- `PageHeader` (eyebrow crumbs via `Crumb`/`CrumbSep`, optional `PageIcon`, `meta`, `actions`), `Card` (`padded` for prose/properties, unpadded for tables/lists), `StatTile`, `Empty`, `Notice`.
- `Properties` is the Notion-style label/value list used on record pages (icons from `src/components/icons.tsx`).
- Tables: `Table` + `Thead`/`Th`/`Tr`/`Td`. Cells are nowrap by default; pass `wrap` for long text and `align="right"` for numbers.
- Forms: `FormSection` (title + description left, 6-column field grid right), `Field`, `Input`/`Select`/`Textarea`/`Checkbox`, `FormActions`, `FormError`. Use `md:col-span-N` on fields instead of empty spacer divs.
- Frame: navy left sidebar (`src/components/sidebar.tsx`, grouped modules, collapsible, role-filtered) + top bar in `src/components/app-shell.tsx` (global search → `/search`, "+ New" menu, attention bell fed by `src/lib/attention.ts`, user menu). Mobile gets a bottom tab bar.
- Record pages use `RecordHeader` + `Tabs` (`?tab=`); list pages use `Toolbar` (GET search + filter chips + count + right-side actions). KPI cards are `Kpi` with `Sparkline`; the only real chart is `src/components/trend-chart.tsx` (two series, blue billable / orange labor, validated for CVD).
- Modules: Home, Owner insights (admin), Needs attention, Clients, Clock, Visits & EVV, Scheduling (roadmap page only), Billing (claim-line prep), Staff, Compliance (matrix), Reports (CSV routes under `/reports/*.csv`), Sites, 245D services, Settings (org form), Audit.
- Record pages use the "workspace" shape: sticky 300px profile column on the left, activity on the right. See `src/app/(app)/clients/[id]/page.tsx`.
- Visits are grouped by **pay period** (`src/lib/pay-period.ts`, biweekly Sun–Sat anchored 2026-08-23). The client timeline shows the current period and loads earlier ones with `?periods=N`; the visits list navigates periods with `?period=YYYY-MM-DD`.

## Data layer
Drizzle ORM, Postgres dialect, PGlite locally (`./data/pglite`, gitignored). Schema in `src/db/schema.ts`; after changing it run `npm run db:generate` and commit the new file under `drizzle/`. Migrations apply at startup in `src/db/index.ts`.

**Every write to a PHI table goes through `audited(db, actor)` in `src/db/audited.ts`.** Never call `db.insert/update/delete` directly from actions. Reads live in `src/db/queries.ts`.

Visits snapshot provider tax ID, MHCP ID, service code, modifiers, and rendering NPI/UMPI at creation. Manual entry and edits are first-class: edits go through `recordEdit` in `src/app/(app)/visits/actions.ts`, which writes the `visit_edits` row and flips `manual_entry` in one transaction.

## Domain rules
- The client identifier is the **PMI #** (`people.pmi`, 8 digits). Never label it MHCP ID in the UI.
- Units are always 15 minutes (`unitMinutes` stays 15). Service codes and modifiers come from `src/lib/hcpcs.ts` (DHS-3945, April 2026); update that file when DHS republishes, do not hardcode codes in components.
- Client signing code: `people.signatureCodeHash` (scrypt, `src/lib/password.ts`) for checking it, plus `signatureCodeEncrypted` (AES-256-GCM) so an admin can read it back. Generated by `setClientCode`. Clock-out requires the code or an "unable to sign" reason; see `clockOut` in `src/app/(app)/visits/actions.ts`.
- Service agreement PDFs are stored under `data/uploads/agreements/` (gitignored) and served through `/agreements/[id]/document` behind auth. AI extraction lives in `src/lib/ai/extract-agreement.ts` (Anthropic SDK, `messages.parse` with a Zod output format, model `claude-opus-5`).

- Caregiver assignments live in `assignments` (staff ↔ person, `orientedOn` = 245D.09 subd. 4a). DSPs can only clock in with assigned, oriented people; enforced in `clockIn` and in the clock UI.
- Staff credentials live in `staff_credentials`; compliance rules are pure functions in `src/lib/credentials.ts`. Add new requirement types there, not in components.
- Service agreements no longer expose Program in the UI (`programId` column kept, unused).

- Staff SSN: `ssnEncrypted` (AES-256-GCM via `src/lib/crypto.ts`, key `DATA_ENCRYPTION_KEY`) + `ssnLast4`. Never select `ssnEncrypted` into a page; use `revealSsn` (admin, audited "reveal" action). Pay rate renders only for admins.
- Client files: `client_documents` rows + files under `data/uploads/clients/<personId>/`, served by `/clients/[id]/documents/[docId]` after `canViewPerson`. DSPs can view only assigned people (clients list, client page, files).

- Owner view (`/owner`, admin only) prices completed visits as units × agreement `unitRate` for revenue and minutes/60 × staff `payRate` for labor. Both are snapshots of *current* rates, not the rate at the time of the visit; when rates change historically, add rate snapshots to visits.

## Documentation, life plan, scheduling, medications
- Service record = the slide-over at `src/app/(app)/visits/record/` (`VisitSheet` mounts on any page via `?visit=<id>`; `VisitRecord` is the server body, `RecordForm` the client form). Saves interaction level, skills (`src/lib/templates.ts` per service group), goal responses, progress review; staff sign → supervisor approve (`approvedAt`). AI draft via `draftProgressReview` uses only the marked facts.
- Life plan: `goals` → `goal_questions` (yes/no prompts) → `goal_responses` per visit. Client tab "Life plan" shows 90-day tallies; caregivers answer in the service record.
- Scheduling: `shifts` (weekly repeat = `seriesId`), eligibility check in `createShifts` (assigned + oriented + compliant + no overlap). Clock-in links the nearest scheduled shift (`findShiftForClockIn`) and moves it in_progress → completed. Scheduled shifts past their end with no visit surface in Needs attention as missed.
- MAR: `medications` (scheduled `times`) and `medication_administrations` (one row per med/date/time; given/refused/held/missed). Client tab "Medical" is the month grid; the service record shows the day's slots.

## Auth
Cookie sessions (`ehr_session`), scrypt password hashes, roles admin / supervisor / dsp. Use `requireUser(roles?)` at the top of every page and action; `can(user, action)` for UI gating. Route group `(app)` is authenticated; `(auth)` is not.

## Conventions
- Preview server: `npm run dev -- -p 3245` (launch config `ehr-dev` lives in `~/Desktop/.claude/launch.json`). Restart it after changing `next.config.ts` or installing packages.
- Scripts run with `tsx` (`npm run db:seed`, `npm run db:reset`).
- Seed data is sample data. No real client information in the repo.
- Dates shown in America/Chicago. Date-only columns are ISO strings; timestamps are `timestamptz`.

## Notes, history, and authorizations (Sept 2026)
- "Visits" are called **Notes** everywhere in the UI (`/notes` rewrites to `/visits`). The client Notes tab (`clients/[id]/notes-tab.tsx`) is document-style, filterable by service code and date range, and exports the same filter as a PDF at `/clients/[id]/notes.pdf` (@react-pdf/renderer).
- Saving documentation records `noteSavedAt/By/Lat/Lng` (device geolocation, best effort). The client History tab lists only these note events via `listNoteEvents`; record edits stay in the audit log.
- Authorizations are editable at `/clients/[id]/agreements/[agreementId]` (`updateAgreement`, audited). Units, dates, rate, status, code, and modifiers can change; history is shown beside the form.
- `people.medicationSupport` gates the Medical tab; `people.status` is changed from the header `StatusControl` (`setPersonStatus`, sets `dischargedOn`).
- Never truncate labels in `Properties`; labels wrap.

## Hosting (Sept 2026)
- Production: https://ehr-system-eight.vercel.app (Vercel project `ehr-system`, deploys from `main` on every push). Database: Neon Postgres project "245D EHR". `DATABASE_URL` selects Postgres; unset means local PGlite.
- `npm run build` runs `scripts/migrate.ts` first, so schema changes ship with the deploy. Uploaded files live in `stored_files` (bytea), never on disk.
- `npm run db:reset` refuses to wipe a hosted database unless `CONFIRM_RESET=yes`.
- Vercel env vars: `DATABASE_URL`, `DATA_ENCRYPTION_KEY`, `ANTHROPIC_API_KEY` (optional `ANTHROPIC_WORKSPACE_ID`). Vercel Authentication is off; the app has its own login.

## Layout: Neon console shape, Hubble palette (Sept 3, 2026)
- The user wants console.neon.tech's *format* only. Palette and fonts stay Hubble (default `data-theme="hubble"`, choice stored in `localStorage["ehr.theme.v2"]`). There is no dark theme; do not add one.
- Shell: breadcrumb top bar (`topbar-crumbs.tsx`: org [245D] / section [role]), centered ⌘K search, status pill ("All OK" / "N to review" → /attention). Sidebar: WORKSPACE label + filled CTA (`nav-cta` tokens), PAY PERIOD switcher, hairline-separated groups, dismissible "Set up your agency" card, "Collapse menu".
- Office dashboard: `GetStarted` row (dismissible), 4-up metric strip with ⓘ hints and a footnote, `ActivityChart` (units per day) beside "Shifts today", then Documentation review and Recent notes. Calendar blocks are translucent tints with a solid left edge.
- Pair `bg-gray-800` with `text-gray-100` and `bg-primary` with `text-primary-foreground` rather than `text-white`.

## Daily Service Note PDF (Sept 3, 2026)
- `clients/[id]/notes.pdf/notes-pdf.tsx`, one note per page. Header table: client + PMI/DOB, date (MM/DD/YYYY), time, hours, units; service, service code, caregiver, setting. Body: narrative, supports, outcomes on the left; level of assistance, medication administration, incidents, visit verification in a right sidebar (user preference); then DocuSign-style script signatures (Great Vibes) for caregiver and client with the attestation language under them. No supervisor line, no agreement number, no HCPCS/units-authorized line, no statute paragraph. Hours, never minutes.
- Fonts for PDFs are registered in `src/lib/pdf-fonts.ts` from `src/fonts` (Public Sans, Fraunces, Inconsolata, Great Vibes) and traced into the bundle via `outputFileTracingIncludes`. Call `registerPdfFonts()` in every PDF route. The library's `render` prop on fixed footers does not draw here; use static text.
- `scripts/set-sample-note.ts <file>` swaps a long narrative onto Jordan Abelard's latest H2014 note for layout checks.
- Daily activities (Sept 3, 2026): `visits.activities` (text[]) chosen from the person's library (`people.activityLibrary`, empty = `DEFAULT_ACTIVITIES` in `src/lib/templates.ts` with `{name}` filled). Picker is a dropdown + removable chips in `record-form.tsx`; supervisors edit the library on the client's Life plan tab (`activity-library.tsx`, `setActivityLibrary`). Printed under Level of assistance on the note.
- Service summary (Sept 2026): `notes.pdf/timesheet-pdf.tsx` prepends a landscape biweekly timesheet to the notes PDF whenever the requested range covers 7+ days. One page per caregiver and service per 14-day block: dates, time in/out, ratio, activities, units, daily totals, week and period totals, the authorization, and both attestations with signature rows. Single-day and short-range exports get notes only.
- A single note prints from the service record: the PDF button in the slide-over header hits `/clients/[id]/notes.pdf?visit=<id>`, which renders that one note in the same template and skips the summary.
- Page breaks: headings never strand at the foot of a page. Short blocks (supports, sidebar facts, outcome rows, signature blocks) are `wrap={false}`; long sections use `minPresenceAhead` on the heading so it moves to the next page when there is no room for content beneath it.
- Approval model (Sept 2026): a note is **accepted when the caregiver submits it** (`saveDocumentation` sets `approvedAt` on staff sign). Supervisors review in batches and send back the ones that need work with `returnNote(visitId, reason)` → `returnedAt/returnedBy/returnReason`; `acceptNote` clears it. Resubmitting accepts again and clears the return. Nothing locks on approval any more; only voided visits are read-only. Returned notes surface in Needs attention and as a dashboard metric.
- Sidebar groups are WORKSPACE, CARE, then two unlabelled hairline-separated groups. There is no pay-period switcher; the period belongs to the pages that use it.
- Dashboard "Today on the board" (`today-board.tsx`, Sept 2026) replaced the units-per-day activity chart: today's shifts on a 6am–9pm clock, a red line at the current time that ticks each minute, and a "late" flag on any shift that started with no clock-in. Client component; the server passes minutes-past-midnight in Chicago rather than timestamps.
- Clicking a note row anywhere opens `?note=<visitId>`, which `NotePreview` (mounted in the shell) renders as the finished PDF in a dialog, with Open record and Download. The inline single-note route is `/visits/[id]/note.pdf`; `clients/[id]/notes.pdf/build.ts` holds the shared renderer. shadcn's DialogContent carries `sm:max-w-sm`, so any wide dialog must also pass `sm:max-w-none`.
- Needs attention supports bulk fixes (`attention-list.tsx` + `attention/actions.ts`): tick rows in Unsigned visits, Manual visits pending EVV, or Missed shifts and act on them together. Every bulk action resolves the row it targets and is audited; nothing hides a problem. `AttentionItem.id` carries the underlying record so a row can be selected. Unsigned visits only appear when no reason is recorded; manual entries clear once `manualEvidenceAt` is set.
- Owner insights wording: "Profit expectations" (billable minus gross pay) and "What you bill against what you pay". Rates are current, not snapshots taken at visit time.
- Usability pass (Sept 5, 2026) against Nielsen's heuristics: user-facing "visit" wording finished migrating to "note" (routes, EVV terms and identifiers keep "visit"); every bulk action asks a plain-language confirm and returns an `undo` payload that `undoBulk` reverts from the toast; missing-key messages say who fixes it and how; the setup card retires itself once all four steps are done.
- Two more usability fixes (Sept 5, 2026): every PDF link is now `DownloadButton`, which fetches the file, shows "Preparing…", and toasts on failure instead of a silent link. `src/components/rule.tsx` holds short plain-language explanations of the 245D rules the app enforces (orientation, units, signing code, manual entries, planning deadlines, documentation, incidents) with statute cites; drop `<Rule name="…" />` wherever a rule bites. `Card` and `FormSection` take a `titleAfter` slot for it.

## UI/UX refresh (September 5, 2026)
The latest user authorized palette changes. Default theme is now Tide with slate surfaces and teal primary actions; existing saved themes remain available. Headings use Public Sans; font assets are loaded locally. Navigation groups are Workspace, Care, Operations, Administration. Needs attention is labeled Review queue. Its existing data source and audited actions remain authoritative; every attention kind, including returned notes, must render. See UI-UX-UPDATE.md for implemented behavior and validation limits. Keep planning date calculations distinct from verified completion.
- Type (Sept 5, 2026): the interface is Montserrat, loaded locally at 400 and 500 only (`src/fonts/Montserrat-*.ttf`). `font-synthesis-weight: none` plus a rule mapping semibold/bold/headings to 500 keeps the browser from faking a weight the family does not have here, so emphasis comes from size, colour, and spacing. Base stays 14px. The PDFs are unaffected: they register their own faces in `src/lib/pdf-fonts.ts` (EB Garamond, Great Vibes) and the printed note is unchanged.
- Palette (Sept 5, 2026): the Tide default runs a clinical blue (`--primary: #1d4e89`, hover #173e6d, soft #dce7f5). Green is reserved for status: signed, clocked in, completed. Two blocks define Tide, the base one and the later "Care workspace refresh" override; change both or the later one wins.
- "Owner insights" is now **Agency performance** (`/owner`), subtitled by what it actually shows. Its duplicate attention list is gone; only the three counters remain, under "Where the risk sits", pointing at the review queue. The dashboard's "Next in review" card is gone for the same reason: the review categories above it and the queue itself already cover it.
- Signing codes (Sept 5, 2026): `issueClientCode` in `src/lib/client-code.ts` is the only place a code is minted. It stores the hash, texts the code when the client has a mobile number and Twilio is configured, and records `signatureCodeSentAt/SentTo`. `createPerson` calls it, so a new client always leaves with a code; the plain code rides back on `?code=` for a one-time notice and is never stored. `/api/cron/rotate-client-codes` rotates codes older than `CODE_ROTATION_DAYS` (14) for active clients with a phone and is scheduled weekly in `vercel.json`; it needs `CRON_SECRET` and does nothing when texting is off. SMS lives in `src/lib/sms.ts` (Twilio REST, no SDK).
- Billing filters by service code with chips above the tables (`?code=`), counts included.
- Texting a signing code requires `people.smsConsent`. The client form carries the checkbox next to the phone number; `issueClientCode` and the rotation job both refuse without it and say why. Turn texting on with the Twilio variables in `.env.example`; the code panel states which of the three conditions is missing.
- A pay period that has not started shows "This pay period has not started" instead of zeros, and the trend chart plots only periods that have begun.
- The client record dropped the Activity and History tabs (both duplicated the Notes tab and the audit log). "Life plan" is now "Support plan goals" everywhere, including the service record. Goal categories are free text: the picker offers the defaults plus any this client already uses, and "Add a category…" swaps in a text field.
- Notes list: rows open the printed note (`?note=`), not the slide-over, and the data is pre-sorted newest first so sorting by any other column keeps each group in date order (TanStack sorting is stable).
- Shifts repeat by weekday: tick Sun–Sat chips plus a number of weeks. `createShifts` builds every chosen weekday in every week from the week of the start date, skipping dates before the start, past the agreement end, or where the caregiver is already booked. No chips means the weekday of the date entered.

## Navigation: role-shaped top bar (September 7, 2026)
The 16-item sidebar is gone. `src/lib/nav.ts` is the single source of truth for every destination, so the top bar, the section row, the gear menu and the phone's tab bar cannot disagree about what a role may open.
- **Top bar** (`top-nav.tsx`, `TopNav`): admin gets six — Today, Clients, Schedule, Notes, Billing, Review; supervisor five (no Billing); caregiver four — Today, My clients, Clock in, My notes. Only Review carries a red badge. Labels hide below `lg`, the whole bar below `md`.
- **Section row** (`SectionNav`, sticky under the header): appears only where a section has depth — Clients, Notes and the review queue. Today, Schedule and Billing return null from `sectionRow()`, and caregivers never get one. Its counts come from `attentionItems()` via the layout, so the row and the bell always agree and nothing extra is queried.
- The row *is* the filter. `/clients?status=`, `/visits?state=` (unsigned, returned, manual, open) and `/attention?kind=` are real query params; `ClientsTable` takes `showChips` (true only for caregivers, who have no row) and `VisitsTable` takes `state`, falling back to its own chips when absent.
- **Gear menu**: eight entries in two groups, Run the agency (Agency performance, Staff, Compliance, Reports) and Set up (Sites & programs, 245D services, Settings, Audit log). Caregivers get no gear; their profile and the service reference stay in the avatar menu.
- **`/agreements`** is new: every authorization with units used, percent consumed and end date, sorted by expiry. It answers "which ones run out this month", which the client record cannot because it holds one person at a time.
- `isoDay(offset)` in `src/lib/format.ts` exists because the React Compiler lint rejects a bare `Date.now()` during render.
- Untouched on purpose: the client record's own tabs. Moving the rail and reorganising the record in one pass makes a regression impossible to attribute.

## Chart record and the pinned rail (September 7, 2026)
The client record was nine identical cards; it is now a chart. `src/components/chart.tsx` holds the primitives and nothing else should hand-roll them.
- `PatientBanner` — the soft band that carries name, PMI, DOB/age, waiver, county, status chips and the actions. Its colour is `--banner` (#dce7f5, the same tint as an active nav tab) over `--banner-line` (#b9cee8); the tokens exist because `--nav` is white in Tide and `bg-nav` renders invisible. Text on it is full-strength ink, never a tint — the fact values are what someone glances at. A dark navy banner was tried and rejected in September 2026: a second blue next to #1d4e89 reads as a mistake. Small round avatars (rail, care team) take `bg-primary`, not `--banner`, because white initials vanish on the pale tint.
- `ChartGrid` / `ChartCol` / `ChartSection` / `ChartLine` / `ChartFacts` — three columns divided by hairlines, section labels at 10.5px uppercase. **Do not use `<h1>`/`<h2>` for these labels**: unlayered `h1, h2 { font-size }` rules in globals.css beat Tailwind's layered utilities, so a heading silently renders at 22px.
- Right column is `ChartAlert`s computed on the page: no signing code, no mobile number, unsigned notes, no active authorization, an authorization ≥75% used or ending within 60 days, and any caregiver not yet oriented. Each carries the link that fixes it. If nothing is wrong the column says so.
- `serviceColor(code)` assigns one of six hues per service code, drawn as `ServiceDot` and `UnitBar` so the same service reads the same colour on the authorization and on every note below it.
- "Recent notes" looks back 90 days, not one pay period, so the list is never empty just because the period turned over. Only notes that need something (unsigned, manual, in progress, void) get a badge; a signed one gets a check.
- `ClientRail` (`clients/client-rail.tsx`, mounted by `clients/layout.tsx`) pins the caseload beside every screen under Clients, with a red dot where a person has no signing code. It hides itself on `/clients`, where the table is already the list, and on phones. The section row is no longer sticky so the rail has a clean top edge.
- Identifiers are monospaced: IBM Plex Mono 400/500 (`src/fonts/IBMPlexMono-*.woff2`) drives `--font-mono`, and both `.tabular-nums` and the new `.ident` class use it. PMI numbers, HCPCS codes, unit counts, money and dates in dense columns take it; prose does not. `fmtDateNum` (MM/DD/YYYY) and `fmtDayTime` (MM/DD 3:30p) exist so dates hold one line in a narrow column.
- Login is optional (Sept 2026). `loginRequired()` in `src/lib/auth.ts` reads `REQUIRE_LOGIN`; unset, `getCurrentUser()` falls back to the first active admin so the hosted link opens with no password, and the header shows an amber "No password" marker. `getSessionUser()` is the strict cookie lookup and is what `/login` checks, so signing in as a supervisor or caregiver still works with the login off. **Set `REQUIRE_LOGIN=1` in Vercel before any real client data goes in.**

## Client Profile tab (September 12, 2026)
Modelled on Passage Health's client profile, from a screen recording the user supplied. The idea worth copying is that the administrative record is a **checklist**, not a pile of cards: you can see what is missing without opening anything.
- `clients/[id]/client-profile.tsx` renders a pinned General information card, a section list, and one section's entity cards on the right. Each section row carries a count, a green tick once it has content, and a red `!` when something is wrong rather than merely absent. The Profile tab's own badge is the number of sections still unticked.
- Ten sections: Emergency contacts, Care team, Signing code, Medical information, Case manager, Funding sources, Care locations, Authorizations, Planning · 245D.071, Availability. Five are backed by new tables and editable in a drawer; the rest render existing data and link out to where it is managed.
- Adding and editing happen in a right-hand `Sheet` — the Passage pattern: only the fields that matter, red asterisks on required ones, a solid Save and a plain-text Cancel, then a toast. You never leave the record.
- `profile-actions.ts` handles all five editable sections through one `SECTIONS` map. Every write is `audited`; every edit and delete first calls `ownsProfileRow` because the row id comes from the client. `isPrimary` / `isDefault` are exclusive per person — saving one clears the others.
- New tables (migration `0011_client_profile`): `client_contacts`, `client_funding_sources`, `client_locations`, `client_availability`, `client_diagnoses`. The migration back-fills `client_contacts` from the old `people.emergency_contact_*` columns, which stay on the table but are no longer read by the UI.
- The old "Care team & contacts" tab is gone; its content lives in Profile. The Overview's Emergency fact now reads `profile.contacts[0]`.
- Empty sections say what is missing and why it matters ("No funding source recorded. Add the payer behind these authorizations so claims know where to go."), never "0".
- **Section titles are `<div>`, not `<h2>`.** Same trap as the chart primitives: unlayered `h1, h2` rules in globals.css beat Tailwind's layered utilities and silently render them at 22px.
- Passage's icon rail and module panel were drawn but **not** built: they are a third navigation layer and contradict the September 7 top-bar decision. The pinned `ClientRail` already does most of what their panel does.
- **Navigation moved to a left icon rail (Sept 12, 2026)**, at the user's request, after seeing Passage Health. `src/components/side-rail.tsx` is a 56px sticky rail reading the same `primaryNav()` as the phone tab bar: logo, destination icons with the label in `title`/`aria-label`, a red count on Review, and a `footer` slot the shell fills with the gear and account menus. `TopNav` is retired; the header now carries only the org name, ⌘K, Clock in, + New and the "No password" marker. Below `md` the rail hides and `MobileNav` takes over. Under Clients this gives the Passage shape exactly: icon rail → `ClientRail` caseload panel → record.
- **Base UI gotcha, cost an afternoon:** `DropdownMenuLabel` throws "MenuGroupContext is missing" unless it is inside `DropdownMenuGroup`. A bare `<div>` wrapper is not enough. The error only fires when the menu *opens*, so the page renders fine and then dies on click — the gear, account and Appearance menus were all broken this way from Sept 7 until Sept 12 because presence was verified but opening never was. **Open every menu you add.**
- **Rail tooltips and the module panel (Sept 12, 2026).** Rail icons carry their label in a `group-hover` span rather than `title`: the native tooltip is slow and unstyleable, which makes an icon-only rail feel unlabelled. The active icon also gets a small dot on its right edge. `ClientRail` became the module panel Passage shows — header with icon, name and a close ✕, a search field, the caseload, and `+ Add client` pinned at the bottom. It now shows on `/clients` too (it used to hide there), so the list appears both as the panel and as the sortable table; the panel is for moving between people, the table for scanning columns and exporting. Its visibility follows the route: shown on /clients, hidden the moment a record is open so the record gets the full width. There is no close button, no collapsed strip and no stored preference — the rail Clients icon is how you get back to the list.
- **Clients matches the reference exactly (Sept 12, 2026).** The section row is gone from Clients — `sectionRow()` no longer answers for `/clients`, and the status filters moved into the panel behind a filter button beside the search, where they filter the list they belong to. `/clients` itself no longer renders a table: the panel is the list, so the main area is a centred "Welcome, <name>" with the caseload count, exactly as in the recording. Authorizations moved into the gear menu, which is now its only door. **The sortable caseload table is gone** with its column sorting and CSV export; if that capability is wanted back it needs a home that is not this screen.
- Profile detail matched to the reference: entity card fields stack in one column rather than a three-column grid, the add control is outlined (the page's primary action is Clock in), and a chevron sits on the divider to collapse the General information column.
- Panel rows show the name only — the PMI under every row was noise when the name is what you pick from. It is still matched by the panel's search. Section rows in the profile match the reference: 13.5px label, roomier padding, a circled tick (`Icon.checkCircle`) rather than a bare check, and pill counts.
- `.next/types` sometimes collects Finder-duplicated `"… 2.ts"` files that break `tsc` with duplicate-identifier errors. They are not source; delete them.
- **Profile history (Sept 12, 2026)**, the last section, from the reference recording. `listProfileHistory` in `profile-queries.ts` builds it from `audit_log` rather than a second history table, so it cannot drift from what was actually written: rows are matched by `personId` inside the audit snapshot, which is why `audited()` stores the whole row. Renders as Date · Team member · Event with a "1 — N of N events" line, newest 50. It never counts toward the Profile tab's outstanding badge — a log is not a thing to complete.
- A `blanks` entry of `""` means the section draws its own body from `extras`, so the empty-state line is suppressed. Anything else (or absent) shows the message.

## Matching Passage Health (September 12, 2026)
The user asked for the client area to match the reference exactly. Done so far:
- **Palette is Passage's teal**, sampled from the recording: `--primary: #0098c0`, hover #0081a5, soft #dff3fa. The banner is white (`--banner: #ffffff`) with a hairline, as theirs is. Both Tide blocks changed. **Note:** white on #0098c0 is about 3:1, below WCAG AA for small text — it is the reference's own choice, carried over deliberately.
- **Record tabs renamed** to Overview · Programming · Sessions · Documents · Medication · Profile. (The reference calls its fifth tab "Reports"; ours holds the MAR, so mapping it to that label was wrong and was corrected Sept 12.) The standalone Authorizations tab is gone; the Profile section covers it (`?tab=authorizations` still resolves).
- **Profile sections are exactly the reference's nine**: Emergency contacts, Care team, Medical information, Referring Agency, Funding sources, Care locations, Authorizations, Availability, Profile history. "Case manager" became "Referring Agency" (the user's wording, Sept 12 — the reference says "Referring providers", but a 245D referral comes from a county or lead agency, not a provider).
- **Two 245D controls the reference has no slot for moved rather than being deleted:** the signing code is now a `ChartSection` on the Overview chart (245D requires the client co-sign), and planning deadlines stay on the chart where they already were. Do not remove these to chase the reference — they are licence requirements.
- No ID chip: the reference shows a sequential "ID: 1" we have no field for, and inventing one would be fabricated data. PMI already sits in the banner and is the real identifier.
- **Type is Plus Jakarta Sans** (`src/fonts/PlusJakartaSans-*.woff2`, 400/500/600/700), identified from the recording by its letterforms — double-storey `a`, single-storey `g` with a hooked tail. Because four real weights exist, the old clamp that flattened every heading to 500 is gone: `.font-semibold`/`th` take 600, `.font-bold`/`strong` take 700. Base moved 14px → **15px** to match; record tabs 16px, client name 21px/600, section rows 15px, field labels 13.5px over 15px values.
- The banner carries only the name, "Client since <date>", the status chips and the organisation, as the reference does. PMI, DOB and waiver live in the General information card rather than being repeated in the band.
- **Overview stays our clinical chart** — the user confirmed on 12 Sept they want it kept rather than replaced by the reference's "Target activity" view.
- **Still different from the reference:** Sessions lacks Export notes / Filters / Create session; Documents is a flat category list, not Folders + Files with the per-role permissions dialog; Reports is agency-level, not per-client with "+ New report"; Authorizations has no inner Authorizations/Hours/Units tabs or projected-utilisation bars.
- **No top bar (Sept 12, 2026).** The header with search, Clock in, + New and the password marker is gone; the client banner starts at the very top of the page, as the reference does. `CommandPalette` stays mounted inside an `sr-only` wrapper so ⌘K still opens, and the open-access warning became an amber dot in the rail footer. Record tabs are light-blue pills, not an underline.
- The banner carries only avatar, name and "Client since". Status moved into the General information card (still the interactive `StatusControl` for managers), which is now a 360px column with 24px padding — the reference gives it far more room than a 290px card did. Entity edit/delete buttons are 40px with 20px icons.
- Removed the "Call first" badge on primary contacts: it was my wording and it read as meaningless. The legal-representative badge stays and now says "Legal representative" in full.
- Banner is a rounded bordered card at the top of the record: grey avatar, name, "Client since <date>", the live `StatusControl` pill, a PMI pill where the reference puts its sequential "ID", the no-signing-code badge, and the organisation hard right. Status lives here only — do not repeat it in the General information card.
- Profile columns are `420px` / rest. The General information card is a borderless `bg-panel rounded-2xl p-6`; section rows run the column's full width with hairline dividers and a 4px left bar when active.
- **Client photos (Sept 12, 2026).** `people.photoPath` + `photoUpdatedAt` (migration `0012_client_photo`); bytes live in `stored_files` like every other upload, so nothing touches the filesystem. `photo-actions.ts` validates type (JPEG/PNG/WebP) and size (5MB) server-side, writes a fresh path per upload so a replaced photo cannot be served from cache, deletes the old bytes after the row is updated, and is audited. `/clients/[id]/photo` serves it behind `canViewPerson` with `Cache-Control: private` — a photo is PHI and never touches a shared cache. `ClientPhoto` renders the avatar, the camera badge, and a Replace/Remove menu; no photo falls back to initials. Shown in the banner and the caseload panel.
- Rail tooltip details: it is suppressed on the **active** icon (its label would land on top of that module's own panel, and you already know where you are), and the rail carries `z-30`. Without that the tooltip paints *under* the caseload panel — `sticky` makes the rail its own stacking context, so a `z-50` on the tooltip inside it counts for nothing against a sibling. The active pill is the only "you are here" marker; an extra dot beside it read as a rendering glitch.
- Page background is **white** (`--page-bg: #ffffff` in Tide); filled cards use `bg-card-soft` (`--card-soft`, #f1f4f8 in Tide, a `color-mix` fallback on the base `:root` so every theme resolves it). `--panel` is too close to white to read as a card now the page is white.
- Long values wrap: `ChartFacts` sets `[overflow-wrap:anywhere]` and its `dd` is `min-w-0 break-words`. Without it an email or URL runs straight across the column divider into the next chart column.
- `scripts/backfill-codes.ts` (`npm run db:codes`) issues a signing code to any active client without one — additive, re-runnable, audited. `createPerson` has issued codes since Sept 5, but rows created by a direct insert (the seed) could still be missing one. Node scripts that reach `queries.ts` or `client-code.ts` must stub `server-only` through `Module._load` and dynamic-import inside `main()`.
- No horizontal rule under the record tabs and none above the profile columns — the only divider on that screen is the **vertical** one between General information and the open section. The banner is deliberately compact: `py-2.5`, a 36px avatar and an 18.5px name.
- **General information matches the reference exactly**: five rows in its order — Full name (with a 52px avatar inline, not a row icon), Date of birth, Sex at birth, Address, Departments. **No phone, no email, no PMI, no waiver** on this card: PMI is in the banner, waiver is under Funding sources, and the reference has none of them here. Values are 15px medium over 15px grey labels, row icons 20px in full-strength ink, title 19px semibold, and the date is `fmtLongDate` ("February 20, 1975") in the sans face — not the mono `.ident` used elsewhere.
- `people.sexAtBirth` (migration `0013_sex_at_birth`, reuses the existing `gender` enum, nullable) exists because the reference prints it; it is on the client form and shows "Not recorded" when unset.
- The caseload panel has **no manual toggle** (Sept 12, 2026). `ClientRail` returns null when the path names a record, so opening a client gives the record the whole screen; `/clients` and `/clients/new` still show it. Do not reintroduce a close button — the user asked specifically not to have to press anything to see or dismiss the list.
- **Availability is a schedule editor, not a row form (Sept 12, 2026)**, copied from the reference. `availability-editor.tsx` shows the whole week in one drawer: start/end date, time zone, and seven weekday rows that toggle between "Unavailable" and one or more time windows. The copy control on a filled day opens "Copy <Day> times to" with Select all and each other day, then Apply — the point is not typing 2:30–7:00 five times. `saveAvailability` replaces the entire set in one audited pass, because a partial write would leave the record describing a week nobody chose.
- `client_availability` gained `startDate`, `endDate` and `timeZone` (migration `0014_availability_schedule`). They describe the schedule, not the row, and are written identically on every row since the editor always saves the set together.
- Entity cards are capped at `max-w-[680px]` rather than filling the pane, lift to `hover:bg-primary-soft hover:border-primary`, and set their values in `font-semibold text-text-strong` over muted labels. `tel:`/`mailto:` inside a card inherit that ink instead of rendering teal — the underline on hover is affordance enough and the colour was the only loud thing on the card.
- Verifying `:hover` through CDP is unreliable: `Input.dispatchMouseEvent` does not always set the state and `CSS.forcePseudoState` silently did nothing here. Check the compiled stylesheet for the generated rule instead of trusting a computed style.
- **`--color-card` was never mapped in the `@theme` block** (only `--color-card-foreground` was), so `bg-card` generated no rule and all 51 uses rendered transparent. Fixed Sept 12, 2026. It only became visible when a popover needed to cover content behind it.
- `SheetContent` bakes in `data-[side=right]:sm:max-w-sm`. A plain `sm:max-w-[…]` loses to that data-variant, so a wide sheet must override at the same specificity: `data-[side=right]:sm:max-w-[760px]`. Same trap as `DialogContent`'s `sm:max-w-sm`. Pass `showCloseButton={false}` when the sheet draws its own close control, or you get two overlapping ✕.
- When checking whether a utility was generated, grep the served stylesheet for the **pretty-printed** form (`.bg-card {`), not the minified `.bg-card{` — dev CSS is formatted, and a false negative there sent me chasing the wrong bug twice.
- Availability renders as **seven day cards** (Sun–Sat, each showing its windows or "Unavailable") under a "From … to … · Last updated …" line, with a note that times are Central. It is drawn through `extras.availability`, so `entities.availability` is `[]` and `blanks.availability` must be `""` whenever rows exist — otherwise the empty-state line prints above the week.
- Time zone is **Central only**: a 245D licence is a Minnesota licence, so the field is shown, not chosen. `ZONE`/`ZONE_LABEL` in `availability-editor.tsx`.
- The profile's left column is its own scroll container (`lg:sticky lg:top-2 lg:max-h-[calc(100vh-1.5rem)] lg:overflow-y-auto`), so moving between sections scrolls the list rather than the page.
- **The page does not scroll; panes do (Sept 12, 2026).** The shell is `h-screen overflow-hidden`, `main` is `flex min-h-0 flex-1 flex-col overflow-y-auto`, and the record root claims the rest with `flex min-h-0 flex-1 flex-col`. The profile's two columns then each carry `lg:min-h-0 lg:overflow-y-auto`, so the section list scrolls without moving anything else.
- The chain breaks at any ancestor that sizes to content. `clients/layout.tsx` was the culprit: its wrapper needed `min-h-0 flex-1` and its children container `flex min-h-0 flex-col`, or everything below grew past the viewport and was simply clipped by the shell's `overflow-hidden`. **Use `flex-1` + `min-h-0` down the whole chain — `h-full` on a child of a scrolling flex item does not resolve.**
- The divider between the profile columns **is** the collapse control (`client-profile.tsx`): a full-height 20px hit area whose 1px line and chevron both turn primary on hover, matching the reference. Collapsing hides General information and the section list, giving the open section the full width.
- `profile-history.tsx` renders history as the reference does: sortable Date / Team member / Event headers, a bold date stamp (`fmtHistoryAt`, "Sep 12, 2026 · 5:46 PM"), column rules, a "1 — N of M events" line and a rows-per-page select. The sort arrows really sort — a header that looks clickable and is not is worse than a plain label.
- **`people.medicationSupport` really gates the tab body** (fixed Sept 12, 2026): with it off, the Medication tab shows only the "Turn on medication support" prompt — no MAR grid, no discontinue button, no add-medication form. It had been rendering the whole MAR for people who have no medication support at all.
- Profile history is a dense log: 12.5px type, 32px rows, capped at **25 rows a page** with prev/next paging beyond that. `listProfileHistory(id, 500)` feeds it so later pages are real.
- **Fixed-height chrome inside the record needs `shrink-0`.** Since the record became a flex column (for the pane-scrolling fix), any child without it gets squeezed by the profile grid's `flex-1`: the tab strip collapsed from 49px to 15px and clipped the active pill, which read as a rendering glitch. `Tabs` in `kit.tsx` and `PatientBanner` both carry `shrink-0` now.
- **Organisation is "Sonder Homecare"** (the user's company, Sept 12, 2026). It lives on the `organizations` row — `npm run db:org -- "Name"` renames it on a live database, audited and re-runnable. The seed matches, and the app title, manifest and login wordmark no longer hardcode the old name.
- Profile sections are linkable: `?tab=profile&section=<key>` opens that section, so "All →" on the chart's Care team lands on Care team rather than the first section in the list.
- Medications can be **deleted**, not only discontinued, for the case one was added by mistake — but `deleteMedication` refuses once administrations exist and says to discontinue instead. Those doses are the MAR; a medication somebody actually gave is history, not a typo.
- The document upload no longer asks for a "Note for staff": the title already carries it. The medication form's "Instructions for staff" stays — that is where "Hold if blood sugar under 70" lives.
- The admin staff record is **Mustafa Ali** (`admin@example.com`), so the greeting and every care-team list read Mustafa. Renamed on the live database with `npm run db:staff-name -- <email> <First> [Last]` (audited, re-runnable) and matched in the seed. The greeting is not hardcoded — it comes from the signed-in staff record.

## Signing code: a readable reference (September 12, 2026)
- The code was hash-only by design, so nobody — including the owner — could read a client's code back. The user asked for a reference anyway: a code is verbalised to a client, the client forgets it, and staff need to read it out again. **The tradeoff was stated to the user before building it**: a readable code means staff could in principle sign for a client. It is mitigated, not eliminated.
- Stored the way staff SSNs are: `people.signatureCodeEncrypted` (AES-256-GCM, `DATA_ENCRYPTION_KEY`, migration `0015_code_reference`), written by `issueClientCode` beside the hash. The hash stays authoritative for verifying a code at clock-out; the ciphertext is only ever read by a reveal. **Never select `signatureCodeEncrypted` into a page.**
- `revealClientCode(personId)` in `clients/actions.ts` is the only door: admin/supervisor, `canViewPerson`, and `audited(...).event("reveal", personId, "people", { field: "signatureCode" })` on every call. Profile history renders that action as "viewed the signing code", so "who read this code" is answerable from the record itself. A code minted before the migration has no ciphertext — the panel says to generate a new one rather than failing silently.
- Rotation is **7 days** (`CODE_ROTATION_DAYS`), down from 14, because the user asked for weekly. `vercel.json` already ran the cron at `0 14 * * 1` so no schedule change was needed.
- Notification is in-app: `attention.ts` has a `code_rotated` kind for codes set in the last 4 days that were not texted, shown in the review queue as "New signing codes to read out". Texting the client still needs Twilio; until then the queue is the notice, because a rotation nobody is told about means the client fails their next signature.
- The panel (`client-code.tsx`) shows a masked `••••••` chip with a show/hide button, then `Set` and `Rotates` dates and one line on delivery. The old version printed the code once and then nothing useful — the chip is the reference.
- **A malformed `DATA_ENCRYPTION_KEY` crashed the record** the first time Generate was pressed in production (Sept 12, 2026): the key was set in Vercel but not 64 clean hex characters, `encryptField` threw, and the server action took the whole page down with "This page couldn't load". Two changes: `key()` in `crypto.ts` now trims whitespace and strips surrounding quotes before testing the value — a pasted key usually arrives with a newline attached, and an untrimmed key looked identical to a missing one — and its error names the observed **length** (never the value), so the log says "got 65 characters" and you know which mistake it was. `issueClientCode` wraps `encryptField` in try/catch and stores `null`: the hash is what signing checks, so a broken key must not stop a client getting a code. The panel toasts "The code works, but it could not be saved for later reference."
- `vercel logs <url> --project ehr-system` is how a production 500 gets diagnosed; the Next.js digest shown on the error page (`ERROR 2240186455`) matches the `digest` field on the logged stack.

## Type scale measured off the reference (September 12, 2026)
The user recorded a tour of Passage Health's pages and asked for our type to match theirs per page. The sizes below were **measured, not guessed**, and the method is worth keeping because eyeballing a screen recording does not settle a 1px difference:
- Frames come out of the recording at native resolution (3024x1964 = exactly 2x a 1512x982 desktop, so image px ÷ 2 = CSS px). `ffmpeg -ss <sec> -i <mov> -frames:v 1`.
- `scripts/.lines.mjs` (scratch, not committed) reads a crop with `sharp`, thresholds ink at the midpoint between the darkest pixel and the page, groups inked rows into lines and inked columns into words, and returns each run's ink width. **The threshold must be the same on both sides** — a fixed threshold cuts more of the antialiased edge on grey text than on black, which made every grey label measure ~3% small and sent me chasing a font mismatch that was not there.
- The same string is then rendered in our Plus Jakarta Sans on a canvas at 100px in each weight, ink-measured the same way, and the size follows: `size = 100 × (their_ink ÷ 2) ÷ our_ink_at_100px`. Calibrating the pipeline against our own text at known sizes returned 18.99px for a true 19px and 15.03px for a true 15px, so it is good to ~0.2%.
- Matching is on **rendered width**, not on their CSS — if they run 22px with -0.02em tracking and we run 21px with none, the line occupies the same space and reads the same, which is what "the same font size" means on screen.

Measured, with what we now use:
| element | reference | ours |
| --- | --- | --- |
| base / form label / field value | 15px | 15px (unchanged) |
| page title (`h1`) | 25.5–26.4px @600–700 | **26px/700** (was 30px/500) |
| form group heading (`h2`) | 20.6–22.3px @700 | **21px/700** (was 22px/500) |
| card title (`h3`, "General information") | 17.0px @600 | **17px/600** (was 15px/500, and 19px on the profile) |
| record tab | 17.3–17.6px @500 | **17.5px/500** (was 16px) |
| profile section title | 16.7px @600 | **16.5px/600** (was 18px) |
| outlined "Add …" button | 16.5–16.9px @500 | **16.5px/500** (was 14.5px) |
| record banner name | 19.2–19.7px @700 | **19px/700** (was 18.5px/600) |
| banner meta ("Client since …") | 14.3px @400 | **14.5px/400** (was 13.5px, and monospaced) |
| profile section list row | 14.9–15.2px | 15px (unchanged) |
| table column header | 12.8px @500 | 13px (unchanged) |
| table cell | 13.0px @400 | 13px (unchanged) |
| report page title | 18.1–18.8px @700 | matches `h1` on those pages |
Verification: our "Programming" tab and our "General information" now measure **223px and 322px of ink at 2x — the same numbers as theirs, to the pixel**.
- **The reference has no monospaced face.** Phone numbers and dates inside the profile's entity cards, and the banner's "Client since" date, moved to the sans face to match. Codes (ICD-10, HCPCS), member IDs, unit counts and money stay in IBM Plex Mono — that was the user's own earlier decision about identifiers and the reference has no counterpart for them.
- Three headings bypassed the shared scale with their own `text-[…]` and were brought back onto it (Sept 12, 2026): the dashboard greeting (was 28px) and `RecordHeader` (was 24px) now take the `h1` step, and the Clients welcome is **32px/700** — measured off the reference's own landing screen ("Welcome, Samira", 542px of ink at 2x, ours 556px for the longer name, both 57–58px tall). **Measure with a crop taller than the text**: a crop that clips the descenders leaves the width right but the height wrong, which reads as a size mismatch that is not there.
- Still on our own sizes, not measured against the reference: roughly 155 hardcoded spots at 12.5px and below (dense chrome — chips, meta lines, table sublines). The reference's smallest measured text is a 12.8px table header, so anything under 13px in our UI is smaller than anything in theirs.
- **13px is the floor (Sept 12, 2026).** The smallest text measured anywhere in the reference is a 12.8px table header, so every size below 13px in our UI was lifted to it: 222 occurrences across 59 files — `text-xs` and every `text-[9…12.5px]`. This is a rule derived from measured evidence, not a per-element match: the screens it mostly touches (Clock, Billing, Scheduling, Agency performance, the audit log) are not in any recording, so there was nothing to measure them against. **Do not reintroduce a size under 13px** without a measurement that justifies it.
- Lifting the floor cost exactly one layout fix: the "Today on the board" time column was a fixed 120px and 13px mono needs ~135px, so it is now 145px. Everything else was checked for overflow across nine screens plus the three record tabs — `scrollWidth > clientWidth` on every element under `main` — and came back clean.

## Scheduling rebuilt on the reference (September 12, 2026)
From a 101-second recording of Passage Health's scheduling module. The shape is theirs; where our data model could not honestly supply a field, the deviation is listed below rather than faked.
- **`/scheduling` is a participant × day grid**, not a week calendar of time blocks. Rows are the caseload in "Clients" mode and the roster in "Team" mode (`?mode=`), columns are the days. `schedule-grid.tsx` draws both — the row axis is the only difference, so a fix lands on both.
- **Toolbar order is theirs exactly**: title, then `‹ Today <range> ›` as one bordered group, a Daily/Weekly/Monthly select, a Team|Clients segmented toggle, and a gear to settings. Second row: departments, participant search, a clear-filters button, then the bell, the action-items switch, Bulk action, and + New event.
- **`action-items.tsx` is a standing column, not a notification list.** Four fixed categories — Cancellations, Unassigned, Authorization issues, Authorization utilization — each stating in italics that it is empty rather than vanishing. Collapsed it becomes a 56px strip of the same four icons. Their categories map onto ours as: cancelled shifts; shifts whose caregiver is no longer active; shifts outside their authorization dates; authorizations at 75% or more of their units.
- The day header reads **weekday first** ("Sun 6"), today's column is tinted from the header down, and a client with no availability window on a weekday gets the reference's grey "Unavailable" chip (`listAllAvailability` in `profile-queries.ts` fetches the whole caseload's availability in one read).
- **"Departments" are our 245D sites.** A client reaches a site through the programs their authorizations are written against — that is the only link between a person and a site, so the filter goes person → agreement → program → site.
- **Create event is a full page** (`/scheduling/new`), not a drawer, in their field order: event type, date with start and end on one line, multi-day, time zone, recurrence, client, team members, notes, then Location. Two fields are deliberately ours: **Event type lists the client's authorized services** rather than free-text session types, because a 245D event has to carry a billable code; and the time zone is fixed to Central and shown, not chosen.
- **Bulk action** (`/scheduling/bulk`) offers Cancel only. `bulkCancelShifts` sets `status`, `cancelledBy`, `cancelReasonId` and `cancelNote` (migration `0016_schedule_settings`) and is audited per shift. **A cancelled event stays on the schedule in red and can be rebooked** — deleting the row would erase the fact that a visit was meant to happen, which is the first thing a county reviewer asks about. Completed shifts are skipped.
- **Schedule settings** (`/scheduling/settings`) has their three tabs. Cancellation reasons are a real table seeded with the reference's four; a reason already used on a cancelled shift is **retired, not deleted**, so old cancellations still read back. Calendar settings (displayed days, displayed hours) live on the `organizations` row and the displayed days really drive the grid's columns.
- Still different from the reference: the daily view is the same participant grid narrowed to one day rather than their hour-by-hour column; there is no drag-to-move, no "Find time", no group events, and no multi-day events.
- **`useEffect` may not call `setState` synchronously** — the React Compiler lint fails the build on it. Derive instead (the bulk form filters its selection against the current matches) or move the state change into the event that caused it (the settings editors close on submit).
- **The nav rail is 80px with 22px icons** (Sept 13, 2026), measured off the reference rather than nudged: their rail is 79px to the hairline, their glyphs 22px, their active pill about 48px. Ours is `w-20`, `size-12` items, `size={22}` icons, a 40px logo, and the shell's three footer controls (the open-access dot, the gear, the avatar) grew to match. The collapsed Action Items strip is 80px too, so the two columns line up. It was 56px with 18px icons before.
- `scheduling/week-calendar.tsx` was deleted with the scheduling rebuild — nothing imported it once the participant grid landed.
- **Schedule toolbar details from a second recording (Sept 13, 2026).** "Select departments" lists the organisation itself — Sonder Homecare — as the reference lists theirs; the site-based filter is gone. The participant search is a type-ahead over clients and staff together, results under "Clients" / "Team Members" headings each followed by a hairline, and picking one fills the search. The filter button opens the reference's Filters panel (Event types, Event status, Client's care team; Reset filters / Done), written to `?code=`, `?status=`, `?staff=`.
- **Focus treatment on those controls**: the border turns primary and a 4px `ring-primary-soft` halo sits outside it (`FOCUS`/`CONTROL` in `schedule-toolbar.tsx`). globals.css has an unlayered `:focus-visible { outline: 2px solid }` that **beats Tailwind's layered `outline-none`**, so a control that draws its own ring must carry `.own-focus` (also unlayered) or it gets both.
- **Overdue compliance no longer blocks scheduling.** `checkEligibility` returns `{ problem }` for the two statutory stops — not assigned, not oriented (245D.09 subd. 4a) — and `{ warning }` for overdue compliance items. The warning rides on the success message and the Create event form toasts it for eight seconds. The user asked for this on Sept 13 because the hard stop was pushing bookings into a spreadsheet; the compliance page still shows the overdue item and the Review queue still lists it.
- The action-items empty-state lines are **12px italic, by the user's request** (Sept 13, 2026) — the reference measures 13.06px there, so this is the one deliberate exception to the 13px floor. Do not "fix" it back up.
- **Chosen filters sit in the field as grey chips** (Sept 13, 2026), as the reference draws them: `Chip` in `schedule-toolbar.tsx`, used by the departments `ChipSelect` and by the participant search once a name is picked. The chip's ✕ clears that field alone. The search value is passed through **with its casing** (`qRaw`) — lowercasing it for matching broke the chip, which compares against the real name.
- The collapsed Action Items strip uses 26px icons and a 22px chevron, at the user's request; the open panel keeps 16px beside its 15px headings.

## Team module (September 13, 2026)
From a recording of the reference's Team module, which is its Clients module with the roster in place of the caseload. Built the same way here, deliberately file for file:
- **Team is on the rail** (`nav.ts`, between Clients and Schedule, admin and supervisor) with a new org-chart glyph `Icon.team`; Staff left the gear menu. The rail is now seven destinations for an admin, six for a supervisor.
- `staff/team-rail.tsx` is `client-rail.tsx` with the roster: same 248px panel, same header/search/filter row, same avatar rows and pinned add button ("Add team member"). The filter chips are All / Active / Inactive / **Out of compliance**, and the one red dot a row can carry means overdue compliance items — the roster's equivalent of the caseload's missing-signing-code dot.
- `staff/layout.tsx` mounts it under every Team screen and, like Clients, the panel returns null the moment a record is open so the record gets the full width. The rail's Team icon is the way back.
- `/staff` is a greeting, as `/clients` is. **The sortable staff table (`staff-table.tsx`) is gone** with its compliance and rendering-ID columns; `/compliance` is where those are acted on and still has them.
- After adding a route-group layout, `tsc` can fail on `.next/dev/types/validator.ts` disagreeing with `.next/types/routes` about `LayoutRoutes` until a build regenerates them. Run the build, then `tsc` again, before believing the error.
- **Staff documents (Sept 13, 2026).** `staff_documents` (migration `0017_staff_documents`) is the personnel file: bytes in `stored_files` like every upload, the row is the index, served only by `/staff/[id]/documents/[docId]` (office roles, or the caregiver reading their own) with `Cache-Control: private, no-store`. Categories in `src/lib/staff-documents.ts` come from the user's own staff folder — background study, training certificate, orientation record, license/insurance, identification, employment form, tax form, policy acknowledgment, evaluation.
- A credential row can carry its certificate: the "Record a credential" form takes an optional file, `addCredential` files it as a staff document with `credentialId` set (category inferred by `categoryForCredential`), and the table shows a **View** link in a File column. If the file is rejected the credential row is rolled back — a credential with a broken attachment is worse than no row. Deleting a credential leaves the file (`credential_id` is set null, not cascaded).
- **A `"use server"` module may export only async functions.** Exporting a constant from `document-actions.ts` failed the production build ("found object") while `next dev` was fine — the category list moved to `src/lib/staff-documents.ts`. Check with `npm run build`, not the dev server.

## Personnel file: the Compliance tab as the licensor reads it (September 13, 2026)
From a personnel-record checklist a 245D licensor sent the user (245D.095 subd. 3). The tab is now two panes — `staff/[id]/personnel-file.tsx`, the client Profile tab's shape — with the licensor's thirteen items down the left in the licensor's order and the evidence for the chosen item on the right.
- **Every item except the date of hire is a credential type**, one each, so the list is data rather than a hardcoded checklist: `application`, `duties_acknowledgment`, `position_requirements`, `qualifications`, `evaluation`, `background_study_results`, `first_supervised_contact`, `first_unsupervised_contact` joined the enum (migration `0018_personnel_file`), alongside the existing orientation, maltreatment reporting, annual training and background study. The background study is two items because the licensor wants two dates: submitted, and results received from DHS. "Results received" reads as *pending* once the study is submitted.
- **Nothing is satisfied without a document.** `addCredential` refuses a row with no file; `buildPersonnelFile` in `src/lib/personnel-file.ts` marks a row that has no attached document as `undocumented`, which counts as not satisfied. Rows recorded before this rule show that way until a file is attached through the detail pane (`attachToCredential`). The user asked for exactly this: the licensor reads the paper.
- **Training records name the trainer.** `staff_credentials.instructor`; `credentialSchema` requires it for orientation, maltreatment reporting and annual training. The detail pane shows "Not named — the licensor requires it" in red on old rows.
- The engine is pure (`buildPersonnelFile(hireDate, credentials, documents)`) and is the only place the thirteen items are defined. `evaluateCompliance` in `credentials.ts` still drives the roster dot and the Review queue; it knows the new types by label but was not extended with the document rule. If the two should agree completely, fold the document rule into it.
- First supervised / unsupervised direct contact are recorded per employee as the licensor words it, with the person served and the supervisor in the note, and an observation note attached. They are not derived from visits — the user chose recording over inference.
- Extras the agency tracks but the licensor does not list (first aid, CPR, driver's licence, auto insurance, other) sit in a final group and show a grey dash when unrecorded, never a red mark.
- Verified through the real form: recording "Job duties acknowledgment" without a file is refused with "Attach the document that shows this"; with a PDF it flips to ✓, the document chip appears, and the count goes 1/13 → 2/13. The old flat credentials table and `CredentialForm` are no longer rendered.
- Personnel-file adjustments from the user (Sept 13, 2026): the application item reads **"Completed employment application"**; **"Meets position requirements" is the one item a written source can satisfy** without a document (`sourceOk` on its spec; `addCredential` accepts either a file or a non-empty note for that type, and the note is labelled Source); **performance evaluations record a frequency**, quarterly or annually, in `staff_credentials.renew_months` (migration `0019_renew_months`) and the next due date follows it; **first aid, CPR and auto insurance are gone from the file** and from `evaluateCompliance` — the enum values remain so old rows still load, but nothing lists them. The driver's licence and "other" stay as the two extras.
- **Staff record Notes tab: the whole row opens the note** (Sept 13, 2026). `staff/[id]/note-rows.tsx` is a client `<tbody>` whose rows push `?tab=visits&note=<id>` — the `tab` matters, because the old timestamp-only link dropped it and closing the preview dumped you on Overview. The client name inside the row is its own link and stops the row click. `Tr` from kit stays a plain server row; a row that needs `onClick` is written out as a `<tr>` in a client component.
- **Staff Notes tab filters by client and service** (`?client=`, `?code=`, Sept 13, 2026). `note-filters.tsx` writes them to the query string so a filtered list survives opening and closing a note preview; `listVisits` gained `serviceCode`. The option lists come from everything the person has ever written (a second, wider `listVisits`), not from the filtered page — otherwise picking one client would hide the other clients from the dropdown.
- **Staff availability (Sept 13, 2026)** mirrors client availability: `staff_availability` (migration `0020_staff_availability`, same columns keyed by `staffId`), an Availability card on the staff Overview drawn by the shared `components/availability-cards.tsx`, and the *same* `AvailabilityEditor` — it took two optional props, `save` and `field`, so the client page is unchanged and the staff page passes `saveStaffAvailability` and `field="staffId"`. Do not fork the editor; the week looks identical on either side of the visit. The schedule's Team view marks a caregiver's off days "Unavailable" from this table, keyed by row id, the way Clients mode already did for clients.
- Editor controls, for anyone driving it in a test: the day toggles are `button[role="checkbox"][aria-label="Mon available"]` (short day names), windows are `input[aria-label="Mon window 1 starts"]`, and the copy control is `button[aria-label="Copy Mon times to other days"]` with a popover of `label`s (Select all, then each other day) and an Apply button.
- **The staff record has no Clients tab** (Sept 13, 2026). Assigned clients are managed from the Overview card's Manage button, which opens `manage-assignments.tsx` — the existing `AssignmentPanel` inside a right drawer, the availability editor's shape — so assigning someone never leaves the page. The tabs are Overview · Compliance · Notes · Login; an unknown `?tab=` (an old bookmark to `clients`) lands on Overview rather than rendering nothing.
- **The office dashboard is a greeting** (Sept 13, 2026, at the user's request) — "Welcome, <first name>", the same screen `/clients` and `/staff` show. The counters, the "Today on the board" clock strip, the get-started card and the recent-notes list are gone with `today-board.tsx` and `get-started.tsx`; everything they showed is on the screen where it is acted on (Review queue, Schedule, Notes). The caregiver home (`CaregiverHome`, clock-in and today's shifts) is untouched — it is a working screen, not a summary. Earlier AGENTS notes describing the dashboard's metric strip, board and GetStarted row are historical.
- **The soft tint is lavender now, the ink is still teal** (Sept 13, 2026, user's request: "every section that's light blue into very light purple"). Tide's `--primary-soft`, `--nav-active` and `--nav-cta` are `#f1ecfa` and `--nav-cta-border` is `#d6c8f0`, in both Tide blocks. `--primary` (#0098c0) did not change — the user had said "keep the blue" for the ink. `--blue-200` is a palette step, not a fixture, and was left alone.
- **No blue anywhere (Sept 13, 2026, "I don't want to see any blue").** Tide's ink is violet: `--primary: #6d4fc2`, hover `#5b3fae`, `--ring` the same, over the lavender `--primary-soft #f1ecfa`. The whole `--blue-*` palette scale is violet too (so `bg-blue-100` / `border-blue-300` utilities in old code cannot leak blue back), the base `:root` fallback is no longer navy, `--chart-1` (the billable series) is violet, and `serviceColor`'s six hues have no blue among them. Components that named the blue scale directly (`kit.tsx`'s secondary button and inputs, the clock panel, the caregiver home, two "Open"/"Mark oriented" pills) now use the primary tokens. The Passage-teal note from Sept 12 is superseded: **#0098c0 is gone.** The `--blue-*` names are historical; the values are purple.
- **The product is called EVVora** (Sept 13, 2026): app title and title template, web-app manifest, login wordmark, README, `package.json`, and the rail's logo tile, which is now an **E**. "Sonder Homecare" is the *agency* — it stays on the `organizations` row and appears on records and PDFs as the provider, never as the product name. EVV in the name is electronic visit verification, which the whole clock-in flow exists for.
- **No Today icon on the office rail** (Sept 13, 2026). The home screen is a greeting and the logo tile already links to `/`, so the icon was the same door twice. The rail is Clients · Team · Schedule · Notes · Billing (admin) · Review. Caregivers keep their Today entry: their home is the clock-in screen.
- **Service hues carry no purple either** (Sept 13, 2026): with violet as the accent and lavender as the tint, a purple service dot read as interface rather than data. `serviceColor` now cycles green, amber, pink, rust, brown, olive. The same six paint the client chart's dots and unit bars, the note dots, the schedule chips, the month grid and the Schedule settings cards, so a service stays one colour everywhere.
- **Logo (Sept 13, 2026).** The user's wordmark is `public/evvora-wordmark.svg` (near-black `#17131D` EVVORA with a lavender `#9B7CF6` verification stroke under E-VV and a lavender A crossbar; 1360×360). It sits above the login card at 50px tall. The rail tile draws the wordmark's rounded **E path** in white on the violet tile (`public/evvora-mark.svg` holds the same path on its own for anything else that needs a square mark). The logo's lavender (#9B7CF6) is lighter than the app's ink (#6d4fc2) on purpose: it is a brand colour on a mark, not text, and at 3.4:1 it would fail as text.
- **App icon (Sept 13, 2026)** is the user's `EVVORA-app-icon-1024.png` (E + VV with the lavender stroke on a light ground), kept at `public/evvora-icon-1024.png`. Sizes are generated from it with `sharp`, not hand-made: `src/app/icon.png` (512, Next's favicon convention — it emits the `<link rel="icon">`), `src/app/apple-icon.png` (180), and `public/icon-192.png` / `icon-512.png` for the web-app manifest, where the 512 also serves as the maskable icon because the artwork carries its own padding. The old placeholder `icon.svg`, `icon-maskable.svg` and `favicon.ico` are gone. Regenerate all of them from the 1024 if the artwork changes.
- **The rail tile is the app icon** (Sept 13, 2026): `/icon-192.png` in a 40px rounded square with a hairline border, no violet fill — so the tile, the favicon and the home-screen icon are one mark. The lone-E variant from the wordmark (`public/evvora-mark.svg`) is no longer used on the rail; it stays for any future square-mark need.
- **Switching person never goes through the greeting** (Sept 13, 2026, from a recording of the reference). With a record open, the rail's Clients / Team icon is a *button*, not a link: it sets `ModulePanelProvider` state (`components/module-panel.tsx`, mounted in the shell) and the module panel slides in **over** the record — `absolute inset-y-0 left-0 z-30` with a shadow and a ✕ — while the record stays underneath. Picking a row, Add, or the ✕ clears it. With no record open the panels behave as before (part of the page, the greeting beside them). The two layouts are `relative` so the overlay sits inside the content area, beside the rail. Verified: open Jordan → Clients icon → panel over Jordan → pick Riley → Riley's record, panel gone; same for Team.
- **Rail tile size (Sept 13, 2026).** The app icon's artwork occupies only the middle 43% of its height (ink bbox y 288–719 of 1024), so at 40px the E+VV read as a smudge — the user could barely see it. `public/evvora-tile.png` is the 1024 cropped to a 915px square centred on the mark (mark spans 92% of the tile width) and the tile is `size-16` with `rounded-xl`; the mark is now ~55px wide instead of ~33. The favicon and home-screen icons still use the uncropped artwork, where the padding is what a launcher expects.
- **No "Recent notes" on the client Overview** (Sept 13, 2026, user's request). The Sessions tab is the note list; the chart keeps Demographics, Care team, Planning, Authorizations, Needs attention and This pay period. `visitTone`/`fmtDayTime` left the page with it.

- **One typeface everywhere (Sept 13, 2026, user's request: "the same font family, different sizes").** The app loads only Plus Jakarta Sans (`layout.tsx`); Montserrat, Public Sans, Fraunces, Inconsolata and IBM Plex Mono are no longer declared in the shell. In `globals.css` every family alias — `--font-serif`, `--font-mono`, `--font-display`, in both Tide blocks — resolves to `var(--font-sans)`, and `.tabular-nums`, `.ident`, `.figure` and `kbd` set the sans face too. **Tabular figures are kept** (`font-variant-numeric: tabular-nums`) so PMI numbers, unit counts and money still align in columns; only the family changed. The Sept 7 "identifiers are monospaced" note is superseded. The PDFs are untouched: `src/lib/pdf-fonts.ts` still registers its own faces for print, and the woff2/ttf files stay in `src/fonts` for it. Verified with a CDP audit that computed `font-family` on every element resolves to `jakarta` across home, clients, a record, the profile, staff, compliance, scheduling, schedule settings, settings, billing and review.

## Document reading — OCR (September 14, 2026)
The paper that enters the agency is now read, not only stored. `src/lib/ai/read-document.ts` sends a PDF or photo to Claude (`claude-opus-5`, the same path `extract-agreement.ts` has used since September) and gets back the full text plus the fields a form would otherwise be retyped from. There is no separate OCR engine: the model reads the page image, so scans and phone photos work as well as real PDFs. Word documents and HEIC photos cannot be sent and are stored as they are (`readable()`).
- **A read is a draft, never a write.** Every reader returns fields the person confirms in a form; the audit log records that person as the actor. Fields the document filled carry a small "from document" mark so the reviewer knows which values to check. Every prompt says to return null rather than guess.
- **Three readers, one per document kind:** `transcribeDocument` (text + one-line summary, the searchable layer), `readCredentialDocument` (personnel file: type, dates, hours, instructor, issuer, certificate number, DHS determination), `readIntakeDocument` (referral packet / CSSP: identity, PMI, waiver, county, address, case manager, guardian, emergency contact, diagnoses, medications). Add a new document kind as a fourth function with its own Zod schema; do not widen an existing one.
- **Structured outputs allow at most 16 nullable fields per schema** (the API rejects more with a 400 about "union types"). The credential schema is under it; the intake schema is not, so its strings are plain with "" meaning not printed and `nulled()` converts after parsing. Keep that in mind before adding `.nullable()` anywhere.
- **The text layer** lives on the document row: `extracted_text`, `extracted_at`, `extraction_summary`, `extraction_model` on both `client_documents` and `staff_documents` (migration `0021_document_text`), capped at 60k characters. It is PHI under the same access rules as the file — never copy it into a separate index. `src/lib/document-text.ts` fills it: `textLayerFor(file)` at upload (client documents, staff documents), `textLayerFrom(text, summary)` when the form already read the file, and `indexStoredDocument(kind, id, userId)` for files uploaded before reading existed. A failed read never fails an upload.
- **Personnel file:** choosing the file in the Record form (`personnel-file.tsx`) sends it to `readCredentialFile` at once and the fields fill in; the text read there rides back in hidden `extractedText` / `extractionSummary` inputs so `addCredential` stores it without a second read. The band warns when the name on the paper is not this person (`nameMatches`, loose on purpose: "NGUYEN, SAM" matches Sam Nguyen) and when the document looks like a different item than the one being recorded. Verified with a rendered certificate: title, date, instructor, 2.5 h, issuer and certificate number all filled; saved; the certificate number then found the file through search.
- **Client intake:** `/clients/new` has "Start from a referral packet" (`intake-reader.tsx`), which reads the file with `readIntakeFile` and re-keys `PersonForm` with the result as defaults. Diagnoses and medications have no field on that form, so the band lists them with where to add them after saving, and reminds the person to file the packet under Documents. Verified with a rendered county referral: 26 fields filled.
- **Search** (`/search`) now has a Documents card: `searchDocumentText` in `queries.ts` runs ILIKE over the text layer and title on both tables and returns a snippet around the first hit. Every filed document shows a green "Searchable" chip (tooltip = the reader's summary) or, when it has no text, a **Read** button (`components/document-text-chip.tsx`) that indexes it in place.
- **Calling a `useActionState` dispatch from an event handler must be wrapped in `startTransition`**, or React warns and `isPending` never turns on — the spinner did not show until this was done in both readers.
- Gated everywhere on `aiConfigured()`; without `ANTHROPIC_API_KEY` the forms say so and work by hand. Nothing about signatures, handwritten timesheets or medication labels was built: those are the higher-risk reads and were deliberately left for a later pass.

## Electronic Visit Verification module (September 14, 2026)
`src/evv/` is an isolated backend domain for Minnesota EVV, built so EVVora can become a third-party EVV system feeding the HHAeXchange aggregator. Read `docs/evv-architecture.md`, `docs/evv-api.md`, `docs/evv-operations.md` and `docs/evv-hhax-integration-checklist.md` before touching it. The rules that matter most:
- **The 245D note (`visits`) is still the note.** An EVV visit shares the note's id (`evv_visits.id = visits.id` when created through the bridge) and links back via `visit_id`; signature state is never EVV state, and nothing in the module marks a note complete.
- **Every EVV table carries `organization_id` and every query in `src/evv/` is scoped by `EvvCtx.orgId`.** The host is one organisation per database; the module does not assume it. `tenant.test.ts` runs two organisations in one PGlite and proves isolation.
- **Events are insert-only, coordinates are encrypted, versions are snapshots.** `evv_events` and `evv_visit_versions` are never updated; `evv_visits` is the current projection (`version` column). Corrections write from/to into `evv_corrections` and a new version. Void is reversible; there is no delete path (`DELETE` on the void route answers 405).
- **The compliance engine is pure** (`compliance.ts`): visit + two events + policy row + a few looked-up facts → status + reason codes. Tolerances (geofence, real-time window, deadline day, plausibility bounds, retry schedule) live on `evv_policies`; whether a service needs EVV lives on `evv_service_rules` (versioned, seeded from the DHS list in `rules.ts`). Do not put a service-code check or a distance anywhere else.
- **Noncompliant is not unsubmitted.** Every EVV-required completed visit is queued; compliance status, submission status and billing readiness are three columns and stay that way.
- **The HHAX adapter fails closed.** No endpoint, field name or rejection code is invented; with configuration missing — and even with it present, until the official schema mapping is implemented — `submit` returns `blocked`, never `accepted`. The mock adapter is the default and the tests' only aggregator.
- **Queue and reconciliation are cron routes** (`/api/cron/evv-submit`, `/api/cron/evv-reconcile`, `CRON_SECRET`); the queue is `evv_submissions`. Backoff is exponential with jitter; validation rejections wait for a correction; the policy's attempt limit dead-letters into `permanently_failed` with an exception.
- **Tests:** `npm test` (`node --test` + in-memory PGlite with the real migrations; `src/evv/__tests__/`). The harness stubs `server-only` and sets a throwaway `DATA_ENCRYPTION_KEY`. Never point a test at `DATABASE_URL`.
- **Migration `0022_evv`** adds 16 tables and four nullable columns on `client_locations` (`lat`, `lng`, `protected_address`, `ivr_phone` — the geofence needs a point). Rollback is `drizzle/manual/0022_evv_down.sql` plus removing the journal entry. Historical notes are linked only by `npm run evv:backfill` (dry-run by default) and are marked `imported`, never verified.
- A regenerated migration that keeps its name gets a new journal `when`; the postgres migrator keys on that, so a database that already ran the earlier generation will fail with "already exists". Repair with the down script and by deleting the stale `drizzle.__drizzle_migrations` row before rebuilding — never by editing the applied SQL.
- The web clock (`visits/actions.ts`) mirrors into EVV through `src/evv/bridge.ts` (`mirrorClockIn/Out/ManualVisit/Edit/Void`), best-effort and logged without PHI, so the note flow never breaks because of EVV.
- **EVV screens (Sept 14, 2026).** `/evv` is on the office rail (shield icon, between Notes and Billing) with a section row: Review queue · Compliance · Settings (admin) · Integration (admin). `evv/page.tsx` is the one server entry; it builds the `EvvCtx` from `requireUser` + `defaultOrganizationId` and calls the services in `src/evv/` directly — the JSON API is for the mobile app, not for our own pages. The queue is a list of cards (`queue.tsx`), not a table, so it works at phone width; a row opens `?visit=<id>`, which the page resolves with `visitDetail` and hands to `visit-drawer.tsx` (a right `Sheet`, 720px on desktop, full width on phones). Every reviewer action is a server action in `evv/actions.ts` that re-checks the EVV permission and revalidates `/evv`. Labels and badge tones live in `src/evv/labels.ts` (client-safe). Caregivers get a "Visit verification" card on Today (`EvvMine` in `page.tsx`, hidden until they have EVV visits), a "Check location" readiness line on the clock screen (`LocationReadiness` in `clock-panel.tsx`; reads the position once, no tracking), and every note carries an `EvvCard` (`visits/[id]/evv-card.tsx`). The drawer's `useToast` reads its callback through a ref: passing a fresh closure into the effect's dependencies re-fired the toast on every render.
- **Seed is small (Sept 14, 2026, at the user's request):** the admin login (Mustafa Ali), one caregiver (Sam Nguyen), one client (Jordan Abelard) complete in every section, and the EVV records those notes produce through the real services (`createVisit` / `clockIn` / `clockOut` with frozen clocks, the mock aggregator, one rejection, one correction). `SEED_REUSE=1` seeds into a database that already holds the organisation, the EVV configuration and the admin login — after a targeted `TRUNCATE` of everything else (the list is in the Sept 14 chat; keep `organizations`, `users`/`staff` for the admin, `sessions`, and the `evv_provider_*`, `evv_payers`, `evv_policies`, `evv_service_rules` tables). Production was reset that way so the admin's changed password and the real UMPI (A853450800, entered under EVV → Settings and now on the organisation row too) survived.
- **One ink (Sept 15, 2026, user's request: "every text that is gray should be black").** The muted/hint text tokens (`--text-muted`, `--text-hint`, `--muted-foreground`) and the `.text-gray-400…700`, `.text-hint`, `.text-muted`, `.text-muted-foreground` classes all resolve to `var(--text)` in the final block of `globals.css`. Placeholders stay gray (`::placeholder` rule) so an empty field does not look filled; status colours, links and white-on-filled are untouched. The two-level hierarchy comes back by deleting that one block — do not re-add gray text elsewhere.
- **Client Overview is two columns (Sept 15, 2026, user's request).** Left: Authorizations, then Care team in two sub-columns. Right (300px): Signing code first, This pay period, Needs attention. Demographics left the chart (it duplicates the Profile tab) and the 245D.07 planning deadlines left with it — `deadlinesFromServiceStart` is no longer called from the record; the deadlines still exist in `src/lib/compliance.ts` for the review queue. `ChartGrid` takes `columns="two"` for this layout; the three-column default is untouched for anything else.
- **Service agreements can be archived (Sept 15, 2026).** `service_agreements.archived_at/archived_by` (migration `0023_agreement_archive`). Only a non-active agreement (cancelled, expired, exhausted) can be archived; `setAgreementArchived` refuses otherwise, so the record's "active" list never has to reason about it. The client's Authorizations tab shows current agreements in the main card and an **Archived · N** card beneath with Restore; the agency-wide `/agreements` list hides archived rows. Nothing is deleted: the notes billed against an archived agreement still point at it.
- **Support plan goals are outcome-first (Sept 15, 2026, user's request).** `goals.outcome` is the one measurable line from the plan; `goal_reviews` (migration `0024_goal_outcome_reviews`, insert-only) is the supervisor's periodic judgement (on_track / needs_attention / met / not_met + note) and the chip a goal wears is its latest review. **Questions are optional**: `goalSchema.questions` has no minimum, and a goal with none is judged at review alone — the seed's "Give Ingrid a break", "Reduce sundowning episodes" and "Walk to the mailbox" are outcome-only examples. `life-plan.tsx` is a full-width list (Active / Met / Discontinued groups; each row shows title, outcome, category, whether it has per-note questions or is judged at review, last review, target) and opening a goal replaces the list with a full-width detail — Outcome, Questions, Review history, Edit and Review forms — behind an "← All goals" link. The user rejected a side pane: the goal should take the whole screen. A goal's *title* is the short name; the outcome and the description are separate fields, so never paste the description into the title. A "met" review also closes the goal.
- **Client Documents tab is a checklist (Sept 15, 2026, user's pick from four mockups).** `src/lib/client-documents.ts` defines the documents a 245D client record must hold (support plan, IAPP, goals/outcomes, service recipient rights notice, release of information) with cadence and statute; `buildDocumentChecklist` marks each On file / Renew soon / Renewal overdue / Missing from the newest document in its category. `documents-tab.tsx` renders that card first, then "Other files" (medical, other). `document_category` gained `rights`, `release`, `medical` (migration `0025`). The upload form is hidden until Upload or a row's Add is pressed, preset to that item's category. The "Searchable" chip is gone: a read file says nothing; an unread one says "text not read · Read" (`document-text-chip.tsx`, both client and staff files).
- Client documents can be **archived** (Sept 15, 2026): `client_documents.archived_at/archived_by` (migration `0026`). Archived files leave the checklist, the Other files list and the tab count, and sit in an "Archived · N" card at the bottom with Restore; Delete stays separate and destructive. The checklist carries **no green badges** (a file on the row already says it is on file); a missing item tints its whole row `bg-danger-soft/50` and says "Nothing on file"; only Renew soon / Renewal overdue keep a badge.

## Navigation states and typeface (Sep 15, 2026)
- Selected navigation is shown the Gusto way: a 3px primary underline (rounded, `after:` pseudo-element) and no fill; hovering any tab or nav row gives a light tan fill via the `--tab-hover` token (`bg-tab-hover`, defined per theme in globals.css and mapped in `@theme`). Applied to the record tabs (`Tabs` in kit.tsx), the section row and top nav (top-nav.tsx), the icon rail (side-rail.tsx), the mobile bar (mobile-nav.tsx). Vertical lists (client profile sections, personnel file items, client and team rosters) use the tan fill plus a 3px left bar, which is Gusto's sidebar treatment. `bg-primary-soft` stays for filter chips and status tones, not navigation.
- The interface typeface is Figtree (`src/fonts/Figtree-variable.woff2`, OFL, one variable file, `--font-figtree`), the closest open-licence match to Centra No2, which Gusto runs in its product. If a Centra No2 licence is bought, drop its files beside it and switch `--font-sans` in globals.css and the `localFont` call in layout.tsx.
- **Rail tooltips are portaled (Sep 15, 2026).** In Safari the icon rail's hover label, which used to be an absolutely positioned child overflowing the rail, showed as a clipped black sliver at the rail's edge (the user's recording; Chrome and Playwright WebKit did not reproduce it). `side-rail.tsx` now keeps the hovered tile in state and renders the label through `createPortal` on `document.body` with `position: fixed` coordinates taken from the tile's rect, so no ancestor can clip it and no sibling panel can paint over it. The rail's `sticky top-0` also went (`relative z-40`): the frame is `h-screen overflow-hidden` and never scrolls, so sticky did nothing.
